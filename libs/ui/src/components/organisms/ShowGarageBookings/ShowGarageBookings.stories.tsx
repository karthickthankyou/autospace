import type { Meta, StoryObj } from '@storybook/react'
import { ShowGarageBookings } from './ShowGarageBookings'

const meta: Meta<typeof ShowGarageBookings> = {
  component: ShowGarageBookings,
}
export default meta

type Story = StoryObj<typeof ShowGarageBookings>

export const Primary: Story = {}
