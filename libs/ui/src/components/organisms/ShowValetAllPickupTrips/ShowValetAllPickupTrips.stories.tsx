import type { Meta, StoryObj } from '@storybook/react'
import { ShowValetAllPickupTrips } from './ShowValetAllPickupTrips'

const meta: Meta<typeof ShowValetAllPickupTrips> = {
  component: ShowValetAllPickupTrips,
}
export default meta

type Story = StoryObj<typeof ShowValetAllPickupTrips>

export const Primary: Story = {}
