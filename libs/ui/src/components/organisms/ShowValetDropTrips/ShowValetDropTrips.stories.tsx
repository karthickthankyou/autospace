import type { Meta, StoryObj } from '@storybook/react'
import { ShowValetDropTrips } from './ShowValetDropTrips'

const meta: Meta<typeof ShowValetDropTrips> = {
  component: ShowValetDropTrips,
}
export default meta

type Story = StoryObj<typeof ShowValetDropTrips>

export const Primary: Story = {}
