import type { Meta, StoryObj } from '@storybook/react'
import { ShowValetPickupTrips } from './ShowValetPickupTrips'

const meta: Meta<typeof ShowValetPickupTrips> = {
  component: ShowValetPickupTrips,
}
export default meta

type Story = StoryObj<typeof ShowValetPickupTrips>

export const Primary: Story = {}
